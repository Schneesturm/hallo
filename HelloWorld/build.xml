<project basedir="." default="clean" name="HelloWorld">

    <target name="clean">
        <delete dir="build"/>
    </target>
	<target name="tmp">
		<mkdir dir="tmpdir/src"/>
		<mkdir dir="tmpdir/bin"/>
		<copydir src="src" dest="tmpdir/src"/>
		<copydir src="bin" dest="tmpdir/bin"/>
	        <delete dir="build"/>
	    </target>
    <target name="compile">
        <mkdir dir="build/classes"/>
        <javac srcdir="src" destdir="build/classes"/>
    </target>

    <target name="jar">
        <mkdir dir="build/jar"/>
        <jar destfile="build/jar/HelloWorld.jar" basedir="build/classes">
            <manifest>
                <attribute name="Main-Class" value="HelloWorld"/>
            </manifest>
        </jar>
    </target>

    <target name="run">
        <java jar="build/jar/HelloWorld.jar"  fork="true"/>
    </target>

</project>